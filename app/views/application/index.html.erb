<!DOCTYPE html>
<html>
<head>
  <title>AngelList Jobs: Chicago</title>
 
</head>
<body>

<h2>From AngelList API:</h2>
There are currently <%= @count %> jobs posted by Chicago startups.<br/>
Now displaying the first <%= @response["per_page"] %> of them.<br/><br/>

<% @response["jobs"].each do |job| %>
  <%= job["title"] %> at <a href=<%= job["startup"]["angellist_url"] %> target=_new><%= job["startup"]["name"] %></a> 
  <br/>
  Salary ranges from $<%= job["salary_min"] %> to $<%= job["salary_max"] %>
  <br/><br/>
<% end %>


</body>
</html>
